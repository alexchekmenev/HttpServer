#ifndef STORAGE_H
#define STORAGE_H

template <typename T>
struct Storage {
public:
    //Storage();
    /*Storage(Storage& other);
    ~Storage();
    get(int key);
    set(int key, T value);
    remove(int key);
private:
    int size;
    vector<int> keys;
    T* container;*/
};

#endif // STORAGE_H
